const jwt = require('jsonwebtoken');
const common_configs = require('../configs/commons.config');

const {}

const authenticate_handler = ( req, res, next ) => {

}

const authenticate = async (jwtString, level) => {

  const jwtIsValid = jwt.verify(jwtString, )
} 